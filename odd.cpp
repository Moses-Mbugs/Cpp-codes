#include <iostream>

using namespace std;

int main() {
    // simple odd number
    
    int total = 100; // number of odd numbers to count to
    int i = 1; // starting fromm 1
    int count = 0;

    while(count < total){
        if(i % 2 , != 0){
            cout << i << endl;
            count++
        }
        i++;
    }
    return 0;
}
